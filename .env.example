# Required for auth and API (Cognito-only). Copy from Terraform:
#   cd draft2done-infra/terraform && terraform output -raw api_base_url
#   terraform output -raw cognito_user_pool_id
#   terraform output -raw cognito_user_pool_client_id
#   terraform output -raw cognito_identity_pool_id
#   terraform output -raw aws_region
#   terraform output -raw s3_media_bucket_name

# VITE_API_BASE_URL=
# VITE_COGNITO_USER_POOL_ID=
# VITE_COGNITO_CLIENT_ID=
# VITE_COGNITO_IDENTITY_POOL_ID=
# VITE_REGION=us-west-2
# VITE_S3_MEDIA_BUCKET=
